# `.agent/prp/evidence/`

Evidence captured to support PRP acceptance criteria. Includes command transcripts, screenshots
(linked from external storage; binaries discouraged in-repo), API responses, and validation logs.

One file per evidence artifact, named `YYYY-MM-DD-<slug>.md` (or with an extension matching the
artifact). Empty until the first implementation phase requires evidence capture.
